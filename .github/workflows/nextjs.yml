name: (Deprecated) Deploy Next.js site to Pages

# This workflow has been replaced by workflows/deploy.yml
# Keeping minimal stub for backward compatibility / historical runs.
on:
  workflow_dispatch:

jobs:
  deprecated:
    runs-on: ubuntu-latest
    steps:
      - run: echo "This workflow is deprecated. Use deploy.yml instead." 
